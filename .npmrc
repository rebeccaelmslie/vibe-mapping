# Expo / React Native's Metro bundler does not understand pnpm's default
# symlinked, isolated node_modules layout. A hoisted layout (npm/yarn-classic
# style) keeps Metro and the Expo CLI happy across the workspace.
node-linker=hoisted
